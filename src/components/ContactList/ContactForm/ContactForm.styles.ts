import styled from 'styled-components'

/* Criando um componente estilizado para o container do formulário */
export const FormContainer = styled.div`
  /* Definindo o container para exibir seus filhos em uma coluna */
  display: flex;
  flex-direction: column;

  /* Espaçamento entre os elementos filhos */
  gap: 1rem;

  /* Padding ao redor do container */
  padding: 1rem;
`
